# Flights MCP
This project contains small MCP Server with mock flights data for demo purposes.

# Running this locally
You can start the MCP Server in Docker by running run_dockerized.sh on your local environemnt. This will run the container on port 8000.